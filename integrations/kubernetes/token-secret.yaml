# Secret holding the dispatcher bearer token.
# Generate with: openssl rand -base64 32 > token
# Then: kubectl create secret generic celln-dispatcher-token -n celln-system --from-file=token=token
apiVersion: v1
kind: Secret
metadata:
  name: celln-dispatcher-token
  namespace: celln-system
type: Opaque
