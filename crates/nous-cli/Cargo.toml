[package]
name = "nous-cli"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "nous — run agents in hardware-isolated cells."

[[bin]]
name = "nous"
path = "src/main.rs"

[dependencies]
nous-spec = { path = "../nous-spec" }
nous-manifest = { path = "../nous-manifest" }
nous-store = { path = "../nous-store" }
assay = { path = "../assay" }
forge = { path = "../forge" }
pilot = { path = "../pilot" }
warden = { path = "../warden" }
anyhow.workspace = true
clap.workspace = true
serde.workspace = true
serde_json.workspace = true
is-terminal.workspace = true

[target.'cfg(unix)'.dependencies]
libc.workspace = true

# The real backend only exists on Linux, and only there is it wanted. Everywhere
# else `nous` still validates specs and runs the mock proof, and says so rather
# than pretending.
[target.'cfg(target_os = "linux")'.dependencies]
warden = { path = "../warden", features = ["kvm"] }
